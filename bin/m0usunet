#!/bin/bash
# m0usunet - M0usuNet client (connects to Pi server)
PI="pi5"
PI_HOME="/home/jackpi5"

if [ "$1" = "yt" ]; then
    shift
    ssh "$PI" "$PI_HOME/yt.sh $*"
else
    ssh -t "$PI" "$PI_HOME/m0usunet"
fi
